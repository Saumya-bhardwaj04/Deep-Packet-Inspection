# DPI React Dashboard

## Start

```powershell
npm install
npm run dev
```

Open: http://localhost:5173

Backend default URL is set to: http://localhost:8000

## UI capabilities

- View backend status
- Add/remove IP, app, domain, and port rules
- Save rules to engine
- Trigger PCAP processing
- View processing stats and top app classifications
